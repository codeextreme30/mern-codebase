# دليل الإعداد السريع

## خطوات الإعداد

### 1. تثبيت المكتبات
```bash
npm install
```

### 2. إعداد ملف البيئة
قم بإنشاء ملف `.env` في المجلد الرئيسي وانسخ المحتوى من `env.example`:

```env
PORT=5000
NODE_ENV=development
MONGODB_URI=mongodb://localhost:27017/mern-cb01
```

### 3. التأكد من تشغيل MongoDB
تأكد من أن MongoDB يعمل على جهازك أو استخدم MongoDB Atlas.

### 4. تشغيل السيرفر
```bash
npm run dev
```

### 5. اختبار السيرفر
افتح المتصفح أو استخدم curl:
```bash
curl http://localhost:5000/api/health
```

يجب أن تحصل على:
```json
{
  "success": true,
  "message": "Server is running",
  "timestamp": "2024-01-01T00:00:00.000Z"
}
```

## ✅ Checklist

- [x] إنشاء مشروع Node.js
- [x] تثبيت Express.js
- [x] تثبيت المكتبات الأساسية (dotenv, cors, mongoose)
- [x] إعداد سيرفر Express أساسي
- [x] ربط MongoDB
- [x] إعداد Mongoose
- [x] إنشاء ملف .env.example
- [x] إنشاء هيكلية المجلدات
- [x] إنشاء Base Repository
- [x] إنشاء Base Service
- [x] إعداد Response موحد
- [x] إعداد Global Error Handler

## 📁 الهيكلية

```
backend/
├── config/
│   └── database.js          # اتصال MongoDB
├── controllers/             # معالجات الطلبات
├── models/                  # نماذج Mongoose
├── repositories/            # طبقة الوصول للبيانات
│   └── base.repository.js   # Repository الأساسي
├── routes/                  # مسارات API
├── services/                # طبقة المنطق التجاري
│   └── base.service.js      # Service الأساسي
├── utils/                   # أدوات مساعدة
│   ├── response.js          # معالج الاستجابة الموحد
│   └── errorHandler.js      # معالج الأخطاء العام
├── server.js                # ملف السيرفر الرئيسي
└── package.json
```

## 🎯 الميزات

- ✅ سيرفر Express جاهز للعمل
- ✅ اتصال MongoDB مع Mongoose
- ✅ معالجة أخطاء موحدة
- ✅ استجابات API موحدة
- ✅ هيكلية منظمة وقابلة للتوسع
- ✅ Base Repository و Base Service للاستخدام العام
